# Python
__pycache__/
*.pyc

# OS / editor cruft
.DS_Store
Thumbs.db
CLAUDE.md
.mailmap-fix

# LaTeX build artifacts (keep the .tex source and the committed .pdf, drop the rest)
report/**/*.aux
report/**/*.log
report/**/*.out
report/**/*.toc
report/*.aux
report/*.log
report/*.out
report/*.toc

# Rollout videos: large binaries, regenerable with --video
outputs/rollouts/**/videos/

# Human study evaluation: derived, large, and not for annotators
outputs/human_study/comparison/
outputs/human_study/rendered/
outputs/human_study/responses/
outputs/human_study/truth_overlays/
