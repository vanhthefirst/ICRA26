# Python
__pycache__/
*.pyc

# OS / editor cruft
.DS_Store
Thumbs.db
CLAUDE.md
.mailmap-fix

# LaTeX build artifacts (keep the .tex source and the committed .pdf, drop the rest)
report/**/*.aux
report/**/*.log
report/**/*.out
report/**/*.toc
report/*.aux
report/*.log
report/*.out
report/*.toc
